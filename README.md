Mini-Cog Desktop is a lightweight Windows app for running the Mini-Cog cognitive screening flow with guided instructions and simple data entry.
To run it, create/activate your Python environment, install the dependencies, and start the app with `python -m app.main` from the project root.
